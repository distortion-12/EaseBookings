# Client Environment Variables
# Copy to .env.local and adjust values

# Public API base URL for the client (Next.js)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Optional: Auth
NEXT_PUBLIC_AUTH_PROVIDER=local
